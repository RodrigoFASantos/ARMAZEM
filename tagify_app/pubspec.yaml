name: tagify_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0  # Para chamadas à API
  provider: ^6.1.0  # Gestão de estado
  cupertino_icons: ^1.0.8

  # Base de Dados OFFLINE v
  sqflite: ^2.3.0
  path: ^1.8.3
  #########################








dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
  
  # Assets para base de dados offline
  assets:
    - assets/sql/TABELAS.sql
    - assets/sql/DADOS.sql